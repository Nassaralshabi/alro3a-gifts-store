# تشغيل الموقع بصورة مستقلة

## حالة الاستقلال الحالية

الموقع **لا يعتمد وظيفيًا على Shopify**. المنتجات والتصنيفات والطلبات والمحتوى وبيانات التواصل تُدار عبر قاعدة بيانات MySQL داخلية ولوحة إدارة خاصة. تستخدم واجهة الطلب واتساب فقط كقناة متابعة بعد حفظ الطلب في قاعدة البيانات.

## ما يلزم للنشر خارج المنصة الحالية

| العنصر | المطلوب |
| --- | --- |
| استضافة تطبيق Node.js | خادم أو خدمة استضافة تدعم Node.js 22 وتشغيل تطبيق Express/Vite المبني |
| قاعدة البيانات | MySQL أو TiDB مع سلسلة اتصال `DATABASE_URL` مستقلة |
| التخزين | خدمة كائنات متوافقة مع S3 لصور المنتجات والملفات الجديدة |
| المصادقة | مزود مصادقة بديل للوحة الإدارة أو تعديل طبقة المصادقة الحالية لتلائم مزود الاستضافة المختار |
| أسماء النطاقات | نطاق خاص وربط DNS بشركة الاستضافة |
| متغيرات البيئة | سلسلة قاعدة البيانات، أسرار المصادقة، إعداد التخزين، وأي إعدادات مزود خارجي |

## خطوات التسليم التقنية المقترحة

1. اختيار الاستضافة وقاعدة بيانات MySQL مستقلة وربطها بالنطاق.
2. نقل قيم متغيرات البيئة إلى مدير الأسرار في الاستضافة؛ لا تُحفظ أسرار في المستودع.
3. تشغيل ترحيلات Drizzle على قاعدة البيانات الجديدة قبل تفعيل التطبيق.
4. تنفيذ `pnpm install` ثم `pnpm build`، وتشغيل الخادم باستخدام `pnpm start`.
5. استبدال مصادقة لوحة التحكم أو ربطها بمزود الدخول المختار في الاستضافة الخارجية.
6. مراجعة طلب تجريبي وسلة الطلب وفتح واتساب من اسم النطاق النهائي.

> تعتمد التهيئة الدقيقة على المنصة المختارة. يُفضّل تثبيت قرار الاستضافة وقاعدة البيانات أولًا، ثم تجهيز ملفات النشر المناسبة لها بدل إضافة إعدادات افتراضية غير مستخدمة.
