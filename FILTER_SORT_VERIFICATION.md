# تحقق التصفية والفرز

## خط الأساس المنشور

فُتحت صفحة الكتالوج المنشورة على المسار `/shop?filter-check=1`. بعد اكتمال التحميل، ظهر **262 منتجًا** مع أزرار الفئات وخيارات فرز السعر الثلاثة، وكانت الدفعة الأولى تعرض 12 بطاقة مع زر لعرض بقية النتائج.

## الاختبار التفاعلي

يُستكمل في الخطوات التالية اختيار فئة وتغيير ترتيب السعر، ثم توثيق تغيّر عدد النتائج وترتيب البطاقات.

## نتيجة فئة التغليف

اختير زر **«بوكسات وتغليف»** تفاعليًا في النسخة المنشورة، فانخفض عدد النتائج من 262 إلى **43 منتجًا**، وأظهرت البطاقات المعروضة فئة «بوكسات وتغليف» فقط. ثم اختير ترتيب **«السعر: من الأقل للأعلى»**؛ بقيت منتجات هذه الفئة التي تظهر بسعر حسب الطلب بعد منتجات الأسعار المعروفة وفق قاعدة الفرز.

يُستكمل الاختبار على الكتالوج الكامل للتحقق من تغيّر موضع منتج ذي سعر معروف عند تغيير ترتيب السعر.

## نتيجة ترتيب السعر

أعيد اختيار **«الكل»** ثم جرى التبديل تفاعليًا بين خياري **«السعر: من الأقل للأعلى»** و**«السعر: من الأعلى للأقل»**. ظهر منتج «لوحة تصوير تخرج مخصصة» ذي السعر المعروف (**60 د.إ**) أولًا قبل منتجات «السعر حسب الطلب»، وهو السلوك المقصود: تُرتب الأسعار المحددة أولًا وتبقى المنتجات ذات السعر حسب الطلب في نهاية النتيجة. يغطي اختبار الوحدات أيضًا الحالتين التصاعدية والتنازلية مع عدة قيم أسعار.

## قيد بيانات الإنتاج

أظهر الاستعلام عن المنتجات المنشورة ذات السعر الثابت وجود منتج واحد فقط بسعر **60 د.إ**. لذلك يستجيب عنصر الفرز التفاعلي للخيارين التصاعدي والتنازلي، لكن لا يمكن إظهار اختلاف مرئي بين ترتيب قيمتين سعريتين في الكتالوج الإنتاجي الحالي. للتحقق من منطق المقارنة متعدد الأسعار، يغطي اختبار `catalogFilters.test.ts` قيمتي 45 و80 ويثبت أن الترتيب التصاعدي ينتج 45 ثم 80، بينما التنازلي ينتج 80 ثم 45، مع إبقاء منتجات «السعر حسب الطلب» أخيرًا.
