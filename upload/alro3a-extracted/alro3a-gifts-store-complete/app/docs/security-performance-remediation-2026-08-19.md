# معالجة الأمان والأداء — 19 أغسطس 2026

## نطاق المراجعة

شملت المراجعة نقطة الدخول العامة، ومسارات tRPC، وتبعيات خادم الإنتاج الأساسية، وبناء واجهة React، ومساري الصفحة الرئيسية والكتالوج على سطح المكتب والجوال. استُخدمت خدمة `npm.audit_package_versions` عبر Oomol لتدقيق ست حزم خادمية بإصداراتها المثبتة.

## نتائج Oomol ومعالجتها

أظهر التدقيق الأولي أن حزمتين من ست حزم مدققة متأثرتان: `@trpc/server` بإصدار `11.6.0` و`axios` بإصدار `1.12.2`، بإجمالي 29 تنبيهاً. شملت النتائج تنبيه prototype pollution في tRPC، وتنبيهات متعددة في Axios تخص التلوث بالنموذج الأولي، والـ SSRF، ورفض الخدمة.

| الحزمة | الإصدار السابق | الإصدار المعتمد | نتيجة إعادة التدقيق عبر Oomol |
| --- | ---: | ---: | --- |
| `@trpc/client` و`@trpc/react-query` و`@trpc/server` | `11.6.0` | `11.18.0` | لا تنبيهات للحزمة الخادمية المدققة |
| `axios` | `1.12.2` | `1.19.0` | لا تنبيهات |
| `express` | `4.21.2` | لم يتغير | لا تنبيهات |
| `mysql2` | `3.15.1` | لم يتغير | لا تنبيهات |
| `jose` | `6.1.0` | لم يتغير | لا تنبيهات |
| `@aws-sdk/client-s3` | `3.907.0` | لم يتغير | لا تنبيهات |

أعاد تدقيق Oomol بعد التحديث فحص الحزم الست نفسها ونتج عنه **0 حزم متأثرة و0 تنبيهات** ضمن هذه العينة. لا يعادل ذلك تدقيقاً كاملاً لكل تبعية متعدية؛ لذلك تبقى مراجعة الاعتماديات دورية مطلوبة.

بعد استكمال ترقية AWS SDK وExpress وDrizzle ORM، أعيدت عينة Oomol على سبع حزم خادمية أساسية (`@aws-sdk/client-s3` وtRPC وAxios وDrizzle وExpress وJose وMySQL2) وكانت النتيجة **0 حزم متأثرة و0 تنبيهات**.

## طبقات الحماية المضافة

أضيفت طبقة حماية على مستوى Express قبل واجهة tRPC. تمنع هذه الطبقة إفصاح اسم إطار الخادم، وتضع رؤوس متصفح دفاعية، وتفرض حدود معدل مستقلة حسب حساسية المسار.

| التحكم | الإعداد | الغرض |
| --- | --- | --- |
| إخفاء إطار الخادم | تعطيل `X-Powered-By` | تقليل الإفصاح التقني غير الضروري |
| رؤوس الحماية | `nosniff` و`DENY` و`strict-origin-when-cross-origin` وسياسة أذونات للأجهزة | منع التخمين غير الآمن للملفات والتأطير غير المصرح به وتقليل مشاركة المرجع وتعطيل الكاميرا والميكروفون والموقع غير المستخدمة |
| حد معدل عام | 180 طلباً لكل عنوان عميل في الدقيقة لواجهات tRPC | امتصاص الاندفاعات غير الطبيعية مع إبقاء تصفح الكتالوج عملياً |
| حد تسجيل المدير | 6 محاولات لكل عنوان عميل خلال 15 دقيقة | تقليل التخمين المتكرر لكلمة المرور |
| حد إنشاء الطلب | 12 طلباً لكل عنوان عميل خلال 10 دقائق | تقليل الإغراق الآلي لنموذج الطلب |

أكد فحص استجابة الخادم ظهور رؤوس الحماية الخمسة المضافة. كما تغطي اختبارات الوحدة مسار الرؤوس وحد تسجيل الدخول والعودة التلقائية بعد انتهاء نافذة الحد.

## تحسين الأداء وسرعة الاستجابة

أضيف تقسيم ثابت للحزم في بناء Vite للتبعيات المشتركة (`react-vendor` و`motion` و`icons`). انخفض ملف نقطة دخول الواجهة من **831.59 كيلوبايت** إلى **615.93 كيلوبايت** قبل الضغط، ومن **246.36 كيلوبايت** إلى **176.54 كيلوبايت** بعد gzip. لا يعني ذلك أن كل هذه البايتات أزيلت من أول زيارة — إذ ما زالت الحزم اللازمة للواجهة الرئيسية مطلوبة — لكنه يفصل الأجزاء القابلة للتخزين المؤقت ويخفف ملف التطبيق الرئيسي ويجعل التحميل اللاحق أكثر كفاءة.

| اختبار العرض | النتيجة |
| --- | --- |
| الصفحة الرئيسية — سطح المكتب والجوال | السلايدر، شارة الهوية، وأقسام الفئات ظلت ظاهرة وقابلة للتفاعل. |
| الكتالوج — سطح المكتب والجوال | ظهرت الفلاتر والبطاقات والصور دون تعطل بعد التحديثات. |
| TypeScript | ناجح عبر `pnpm check`. |
| اختبارات الوحدة | 7 ملفات و22 اختباراً ناجحاً. |
| بناء الإنتاج | ناجح عبر `pnpm build`. |

## مخاطر متبقية وخطوات متابعة

> لم يُجر اختبار اختراق نشط أو مسح هجومي للموقع المنشور. الإجراءات الحالية دفاعية وتعالج التبعيات ومسارات التطبيق التي راجعناها.

يبقى من المهم تغيير بيانات دخول المدير الافتراضية إن لم يتغير حساب البدء بعد، وتفعيل مراقبة دورية للاعتماديات، والنظر في حد معدل موزع مدعوم بقاعدة بيانات أو Redis إذا نما حجم الزيارات، لأن الحد الحالي داخل ذاكرة نسخة الخادم. كما أن تقرير Oomol الخارجي الشامل للموقع يتطلب توصيل خدمة `Insites` التي لم تكن مرتبطة بالحساب في هذه الجلسة.

## تدقيق الاعتماديات الكامل

استُكملت المراجعة بتدقيق إنتاج كامل عبر `pnpm audit --prod`. انخفضت النتيجة من **1 حرجة و8 عالية و49 متوسطة و10 منخفضة** إلى **0 حرجة و3 عالية و28 متوسطة و5 منخفضة**. كما رُفع AWS SDK وExpress وNanoid وStreamdown، ثم رُفع Drizzle ORM إلى `0.45.2` لمعالجة التنبيه عالي الخطورة الخاص بتعامل أسماء SQL.

التنبيهات العالية الثلاثة المتبقية انتقالية في `path-to-regexp` الذي يأتي مع Express 4، وفي `lodash` و`lodash-es` المرتبطين بمكتبات واجهة خارج مسار المتجر العام. لم يُفرض تحديث رئيسي لـ Express أو Recharts أو Streamdown في هذه الدفعة كي لا يُدخل تغييراً وظيفياً واسعاً بلا مراجعة منفصلة؛ كل التفاصيل الخام محفوظة في `docs/pnpm-audit-production-final.json`.
